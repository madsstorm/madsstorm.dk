require 'html-proofer'
require './test/test_helper'

desc "build and test website"
task :test do
  sh "ruby test/dummy_test.rb"
  sh "bundle exec jekyll build"
  HTMLProofer.check_directory(
    "./_site",
    {
      :disable_external => true
    }
  ).run
end
